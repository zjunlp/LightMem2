{
  "name": "@tokenpilot/artifact-store",
  "version": "0.1.0",
  "private": true,
  "lightmem2": {
    "role": "foundation"
  },
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "@tokenpilot/host-adapter": "workspace:*"
  }
}
