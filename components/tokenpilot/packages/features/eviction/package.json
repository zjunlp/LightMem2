{
  "name": "@tokenpilot/eviction",
  "version": "0.1.0",
  "private": true,
  "lightmem2": { "role": "feature" },
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "@tokenpilot/artifact-store": "workspace:*",
    "@tokenpilot/history": "workspace:*",
    "@tokenpilot/kernel": "workspace:*"
  }
}
