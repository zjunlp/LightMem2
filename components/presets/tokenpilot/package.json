{
  "name": "@tokenpilot/decision",
  "version": "0.1.0",
  "private": true,
  "lightmem2": {
    "role": "preset"
  },
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "@tokenpilot/eviction": "workspace:*",
    "@lightmem2/history": "workspace:*",
    "@lightmem2/kernel": "workspace:*",
    "@tokenpilot/reduction": "workspace:*",
    "@lightmem2/runtime-core": "workspace:*",
    "@tokenpilot/stabilizer": "workspace:*"
  }
}
