# Plan - 2026-04-18

## Current Phase

Not "can reduction run" anymore. Two parallel goals:
1. Keep reduction stable, configurable, observable
2. Make compaction/eviction real `history -> decision -> execution` capabilities

## Current Reality

- OpenClaw is still the only source of truth for requests and session
- EcoClaw strategy: optimize on each request based on OpenClaw's context
  - Reduction: request content rewriting
  - Compaction/eviction: live archive + stub replacement with real benchmark validation needed

## Completed Status

### Reduction -阶段性完成 (Phase Complete)

Done:
- before_call main chain: `repeated_read_dedup`, `tool_payload_trim`, `html_slimming`, `exec_output_truncation`, `agents_startup_optimization`, `memory_fault_recovery`
- after_call observable: `format_slimming`, `format_cleaning`, `path_truncation`, `image_downsample`, `line_number_strip`, `semantic_llmlingua2`
- Pass-level stats chain connected, bench can output reduction reports
- Pass switches, thresholds, passOptions configured
- Archive + placeholder + deferred recovery as common execution atomic capability

Not doing now:
- Immediate full restructure to pure `decision runtime -> execution` main chain
- Large-scale restructuring just for reduction

### Compaction - 主链已接通 (Main Chain Connected)

v1 implemented but not enough real workload verification yet.

### Eviction - 正在进行 (In Progress)

See `eviction-plan.md` for detailed plan.
